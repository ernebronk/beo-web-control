{
    "name": "music_control_app",
    "description": "Bang & Olufsen Web Music Conroller",
    "private": true,
    "dependencies": {
        "express": "^4.16.3",
        "nodemon": "latest",
        "request": "^2.87.0",
        "socket.io": "latest"
    },
    "scripts": {
        "start": "cd client && npm install && npm run build && cd .. && npm install && nodemon server.js"
    }
}
